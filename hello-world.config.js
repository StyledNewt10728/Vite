//I don't know if I did this right, I included a screen shot to the project submission of me doing the steps in terminal as well as these file within github.

import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
});
